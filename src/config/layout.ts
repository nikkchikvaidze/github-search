import { RoutesConfig } from "./routes";

const HeaderLinks = [
  {
    label: "Home",
    path: RoutesConfig.HOME,
  },
  {
    label: "Favorites",
    path: RoutesConfig.FAVORITES,
  },
];

export { HeaderLinks };
